# Ensure consistent dependency resolution
legacy-peer-deps=true
# Disable optional dependencies that can cause platform issues
optional=false
# Use npm's default registry
registry=https://registry.npmjs.org/ 